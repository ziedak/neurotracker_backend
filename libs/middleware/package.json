{
  "name": "@libs/middleware",
  "version": "1.0.0",
  "description": "Unified middleware library for backend services",
  "main": "../../dist/libs/middleware/index.js",
  "types": "../../dist/libs/middleware/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc --watch",
    "test": "jest",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@libs/database": "workspace:*",
    "@libs/messaging": "workspace:*",
    "@libs/monitoring": "workspace:*",
    "@libs/ratelimit": "workspace:*",
    "@libs/utils": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.19.12",
    "jest": "^29.7.0",
    "typescript": "^5.9.2"
  },
  "keywords": [
    "middleware",
    "authentication",
    "validation",
    "rate-limiting",
    "audit",
    "elysia"
  ]
}
