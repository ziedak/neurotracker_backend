FROM node:20-alpine

WORKDIR /app

COPY package.json pnpm-lock.yaml ./

RUN npm install -g pnpm
RUN pnpm install --frozen-lockfile

COPY dist/apps/prediction ./dist

ENV NODE_ENV=production

EXPOSE 3001

CMD ["node", "dist/main.js"]
